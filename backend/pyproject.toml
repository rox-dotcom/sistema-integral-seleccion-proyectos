[project]
name = "sisp-backend"
version = "0.1.0"
description = "backend API for sisp"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115.10", "uuid>=1.30", "uvicorn>=0.34.0"]

[dependency-groups]
dev = ["ruff>=0.9.9"]
